# Copy to `.env` and fill values. Never commit real secrets beyond public SDK keys.

# RevenueCat public SDK keys (dashboard → Project settings → API keys)
REVENUECAT_GOOGLE_API_KEY=
REVENUECAT_APPLE_API_KEY=
